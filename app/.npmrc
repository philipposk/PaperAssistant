# react-pdf-highlighter-extended@8 still declares a react ^18.3.1 peer range,
# but the app runs on react 19 (works at runtime; tests + build pass). A clean
# `npm install` (e.g. on Vercel) otherwise fails ERESOLVE on that stale peer.
legacy-peer-deps=true
